# name: documints - Preview

# # First-cut CI/CD for documints' Cloudflare Worker. Not yet verified against
# # a real run (no Cloudflare credentials available while authoring this) -
# # expect to fix up exact wrangler output parsing after the first PR. Unlike
# # chamfer-studio's preview workflow, there's no D1 database here (documints
# # is a static site, no per-PR resources are provisioned) - so there's also
# # no matching "cleanup" job to delete anything when the PR closes.

# on:
#   pull_request:
#     branches:
#       - main
#     # Broad pre-filter across all workspace packages — the detect-changes job
#     # below narrows this down to "does documints itself need a rebuild",
#     # since it depends on other workspace packages (e.g. @documints/tokens)
#     # that live outside packages/documints/.
#     paths:
#       - "packages/**"
#     types: [opened, synchronize, reopened]

# concurrency:
#   group: documints-preview-${{ github.event.pull_request.number }}
#   cancel-in-progress: true

# defaults:
#   run:
#     working-directory: packages/documints

# jobs:
#   detect-changes:
#     name: Detect documints Changes
#     runs-on: ubuntu-latest
#     outputs:
#       affected: ${{ steps.check.outputs.affected }}
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4
#         with:
#           # turbo --affected needs full history to diff against main —
#           # a shallow checkout makes it fail open (treats everything as
#           # affected), which is safe but defeats the point of this job.
#           fetch-depth: 0

#       - name: Read Node.js Version
#         working-directory: .
#         run: echo "NODE_VERSION=$(cat .nvmrc)" >> $GITHUB_ENV

#       - name: Enable Corepack
#         run: corepack enable

#       - name: Set Up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: "yarn"

#       - name: Install Dependencies
#         working-directory: .
#         run: yarn install --immutable

#       - name: Check if documints is affected
#         id: check
#         working-directory: .
#         run: |
#           npx turbo run build --affected --dry=json > /tmp/turbo-affected.json
#           AFFECTED=$(jq '.packages | index("documints") != null' /tmp/turbo-affected.json)
#           echo "Affected packages: $(jq -c '.packages' /tmp/turbo-affected.json)"
#           echo "affected=$AFFECTED" >> "$GITHUB_OUTPUT"

#   deploy-preview:
#     needs: detect-changes
#     if: needs.detect-changes.outputs.affected == 'true'
#     name: Deploy PR Preview
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       pull-requests: write
#     env:
#       CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
#       CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Read Node.js Version
#         working-directory: .
#         run: echo "NODE_VERSION=$(cat .nvmrc)" >> $GITHUB_ENV

#       - name: Enable Corepack
#         run: corepack enable

#       - name: Set Up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: "yarn"

#       - name: Install Dependencies
#         working-directory: .
#         run: yarn install --immutable

#       - name: Build documints (lib, CLI, and the docs site itself)
#         run: yarn build

#       - name: Upload preview version
#         id: deploy
#         run: |
#           OUTPUT=$(npx wrangler versions upload --preview-alias "pr-${{ github.event.pull_request.number }}")
#           echo "$OUTPUT"
#           URL=$(echo "$OUTPUT" | grep -oE 'https://[a-zA-Z0-9.-]+\.workers\.dev' | head -1)
#           echo "preview_url=$URL" >> "$GITHUB_OUTPUT"

#       - name: Comment preview URL on the PR
#         uses: marocchino/sticky-pull-request-comment@v2
#         with:
#           header: documints-preview
#           message: |
#             ### 🔗 documints preview

#             ${{ steps.deploy.outputs.preview_url }}

#             Rebuilds automatically on every push to this PR.
